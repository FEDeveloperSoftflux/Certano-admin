/**
 * UserManagement specific components
 * Common components have been moved to @/components/common
 */
import SearchBar from './SearchBar';
import FilterDropdown from './FilterDropdown';

// Export remaining components
export {
  SearchBar,
  FilterDropdown,
};

// Default export for convenience
export default {
  SearchBar,
  FilterDropdown,
};
