/**
 * Common components for UserManagement
 * Centralized exports for easier imports
 */
import StatusBadge, { SupportStatusWithIcon } from './StatusBadge';
import LoadingOverlay, { LoadingSpinner } from './LoadingOverlay';
import SearchBar from './SearchBar';
import FilterDropdown from './FilterDropdown';

// Export all components
export {
  StatusBadge,
  SupportStatusWithIcon,
  LoadingOverlay,
  LoadingSpinner,
  SearchBar,
  FilterDropdown,
};

// Default export for convenience
export default {
  StatusBadge,
  SupportStatusWithIcon,
  LoadingOverlay,
  LoadingSpinner,
  SearchBar,
  FilterDropdown,
};
